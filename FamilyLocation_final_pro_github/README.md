# 家人位置 - 最终增强版

- 飞书机器人 Webhook 默认已填入新地址
- 时间改为滚轮选择
- 测试按钮会保存配置并立即发送
- 原生 GPS/网络定位，不依赖 Google 服务
- 高精度优先：最多等待 2 分钟，精度不够不乱发
- 高德地图链接使用 GCJ-02 坐标转换，减少在高德里显示偏移
- 到点短暂启动前台定位服务，发送后自动停止
- 支持开机后恢复定时计划

重要限制：如果用户在系统设置里“强行停止”应用，Android 不允许应用自行复活。需要手动打开一次。
